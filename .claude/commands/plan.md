---
description: Planificar un cambio antes de tocar código (solo lectura)
---

Analizá el pedido: **$ARGUMENTS**

Modo planificación (SOLO LECTURA — no edites todavía):
1. Explorá el código relevante (`kobra/`, `app/`, `realtime/`, `tests/`) y entendé el estado actual.
2. Identificá qué archivos habría que tocar y por qué.
3. Proponé un plan paso a paso, marcando riesgos (¿toca el pipeline? ¿el modelo? ¿datos sintéticos?).
4. Esperá aprobación antes de ejecutar.
